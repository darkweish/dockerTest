version: "3"
services:
  web:
    build: server/
    ports:
      - "4000:3000"
    depends_on:
      - "db"

  db:
    image: mysql
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=secret
      - MYSQL_DATABASE=nono
      - MYSQL_USER=yoann
      - MYSQL_PASSWORD=secret
